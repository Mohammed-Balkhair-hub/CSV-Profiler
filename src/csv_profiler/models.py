class ColumnProfile:
    def __init__(self, name: str, inferred_type: str, total: int, missing)
        self.name=name,
        self.inferred_type=inferred_type,
        self.total=total,
        self.total=total,



    @property
    def missing_pct(self) -> float:
    
    
    def to_dict()