[theme]
base="light" # Ensures the app starts in light mode
backgroundColor = "white"
secondaryBackgroundColor = "#f0f2f6" # A light gray for containers
primaryColor = "#49A4A1" # Example primary color (optional)
textColor = "black" # Ensures text is readable on white